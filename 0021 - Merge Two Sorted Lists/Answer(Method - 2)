        // METHOD - 2
list 1 => 1,3
list 2 => 2,4
        if(list1 != null && list2 != null){
            if(list1.val < list2.val){
                list1.next = mergeTwoLists(list1.next,list2);  // merge(1,2) => merge(3,2) => merge(3,4) => merge(null,4)
                return list1;
            }
            else{
                list2.next = mergeTwoLists(list1,list2.next);
                return list2;
            }
        }
        if(list1 == null) return list2;
        return list1;
