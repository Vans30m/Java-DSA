class Solution {
    public void sortColors(int[] nums) {
        // Method - 1
        int n = nums.length;
        Arrays.sort(nums);
        for(int i=0;i<n;i++){
            System.out.print(nums[i]);
        }
    }
}
